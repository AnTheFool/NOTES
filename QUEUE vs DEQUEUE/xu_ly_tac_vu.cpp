#include <bits/stdc++.h>
using namespace std;

int main(){
    int n; cin >> n;
    vector<pair<string, int>> tacvu;
    for (int i = 0; i < n; i++){
        string s; int x; cin >> s >> x;
        tacvu.push_back({s, x});
    }
    queue<string> done;
    
}